# 版权边界

本仓库优先发布翻译表、导入导出脚本、补丁构建说明、校验值和不包含原版内容的增量补丁。

以下内容不得提交到 Git 历史：

- 游戏主程序及破解文件
- 完整 `data.win` 或完整 Unity/Unreal 原始资源容器
- 原版音频、视频、贴图和字体
- 存档、日志、缓存和用户隐私数据
- 未确认再分发许可的第三方工具

需要分发二进制补丁时，应优先采用差分文件或新增覆盖容器，并通过 GitHub Releases 发布。确需发布包含派生资源的汉化包时，必须确认附件无法独立运行、明确要求用户合法持有对应完整原版游戏，并且不得将大型二进制写入 Git 历史。
